select * from employees


select * from jobs

select * from departments

select
	e.department_id,
	e.*
from employees e 


explain
select
*
from Employees
limit 10

explain
select
*
from Employees


select
	employee_id,
	first_name,
	last_name,
	salary,
	department_id
from employees


select
	employee_id,
	first_name fname,
	last_name as "this is last_name",
	salary + 99 after_add
from employees


select
	first_name,
	last_name,
	salary
from Employees
where salary > 15000

select
	first_name,
	last_name
from Employees
where salary > 15000

select
	first_name,
	last_name,
	salary
from Employees
where salary < 15000

select
	first_name,
	last_name,
	salary
from Employees
where salary >= 12000

select
	first_name,
	last_name,
	salary
from Employees
where salary <> 12000

select
	first_name,
	last_name,
	salary
from Employees
where salary != 12000


select
	employee_id,
	first_name,
	last_name,
	salary
from employees
where 
	first_name like 'S%'

select
	employee_id,
	first_name,
	last_name,
	salary,
	department_id
from employees
where 
	first_name like '_a%'


select
	employee_id,
	first_name,
	last_name,
	salary,
	department_id
from employees
where 
	first_name like '__a%'
	
select
	employee_id,
	first_name,
	last_name,
	salary,
	department_id
from employees
where 
	first_name not like '__a%'

	
--between
select
	employee_id,
	first_name,
	last_name,
	salary,
	department_id
from employees
where 
	salary between 4800 and 8000

select
	employee_id,
	first_name,
	last_name,
	salary,
	department_id
from employees
where 
	salary not between 4800 and 8000


select
	employee_id,
	first_name,
	last_name,
	salary,
	department_id
from employees
where 
	salary > 5000
	and department_id <> 30
	or department_id = 50

select
	employee_id,
	first_name,
	last_name,
	salary,
	department_id
from employees
where 
	(salary > 5000
	or department_id <> 30)
	and salary < 10000


select
	employee_id,
	first_name,
	last_name,
	salary,
	department_id
from employees
where 
	department_id IN (10,20,30)
	

select
	employee_id,
	first_name,
	last_name,
	salary,
	department_id
from employees
where 
	department_id = 10
	or department_id = 20
	or department_id = 30
			
select
	employee_id,
	first_name,
	last_name,
	salary,
	department_id
from employees
where 
	department_id not IN (10,20,30)	
	
	
select
	employee_id,
	first_name,
	last_name,
	job_id
from employees
where 	
	job_id in ('IT_PROG', 'AD_VP')
	
select
	last_name,
	salary,
	commission_pct,
	coalesce(commission_pct, 0),
	salary + (salary * coalesce(commission_pct, 0)) pelna_pensja
	from employees
	
	
select
	last_name,
	salary,
	commission_pct,
	coalesce(commission_pct, 0),
	salary + (salary * coalesce(commission_pct, 0)) pelna_pensja
	from employees
where
	commission_pct is null
	
	
select
	last_name,
	salary,
	commission_pct,
	coalesce(commission_pct, 0),
	salary + (salary * coalesce(commission_pct, 0)) pelna_pensja
	from employees
where
	commission_pct is not null
		
	

create table coalesce_problem (col1 int, col2 int, col3 int);

insert into coalesce_problem values (null,null,1);
insert into coalesce_problem values (null,2,1);
insert into coalesce_problem values (3,2,1);

select * from coalesce_problem
	

select 
	col1,
	col2,
	col3,
	coalesce(col1, col2, col3) result
from coalesce_problem 

select
	employee_id,
	first_name,
	last_name,
	salary
from employees
order by first_name


select
	employee_id,
	first_name,
	last_name,
	salary
from employees
order by first_name desc


select
	employee_id,
	department_id,
	first_name,
	last_name,
	salary
from employees
order by department_id,first_name 

select
	employee_id,
	department_id,
	first_name,
	last_name,
	salary
from employees
order by 3

explain
select
	first_name,
	last_name,
	salary
from employees
order by salary

explain
select
	first_name,
	last_name,
	salary
from employees

explain
select
	first_name,
	last_name,
	salary placa
from employees
where salary > 10000
order by placa

explain
select
	first_name,
	last_name,
	salary
from employees

explain
select
	distinct department_id 
from employees
